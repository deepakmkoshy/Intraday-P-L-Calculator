import 'package:flutter/material.dart';

const cardBoxDecoration = BoxDecoration(
  borderRadius: BorderRadius.all(Radius.circular(5.0)),
  color: Colors.white,
  boxShadow: [
    BoxShadow(color: Color(0x40000000), blurRadius: 4, offset: Offset(0, 0))
  ],
);



